{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "drag",
  "description": "Allow frontend to start native window dragging",
  "windows": [
    "pet"
  ],
  "permissions": [
    "core:window:default",
    "core:window:allow-start-dragging"
  ]
}
